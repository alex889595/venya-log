/* Адреси Apps Script.

   Ключів тут немає навмисно: репозиторій публічний, а ключ приходить у
   посиланні як #k=… і осідає в localStorage браузера.

   dev теж лишається порожнім: свою адресу для розробки зручніше тримати
   локально, щоб вона не світилась у репозиторії. Один раз у консолі:
     localStorage.setItem('venya.api.dev', 'https://…/exec')

   Де взяти адресу: редактор Apps Script -> Розгорнути -> Керувати
   розгортаннями -> URL веб-додатка (закінчується на /exec). */
const CONFIG = {
  api: {
    dev:  '',
    prod: ''
  },
  defaultDays: 30
};
